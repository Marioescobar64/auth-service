// Nombre del espacio de trabajo
namespace AuthService.Domain.Enums;

// Enumeracion de los roles de usuario
// Cada valor tiene un valor numerico

public enum UserRole
{
    User = 0,
    Admin = 1,
    Moderator = 2
}